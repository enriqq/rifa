name: Notificar tickets pendientes

on:
  schedule:
    - cron: '*/5 * * * *' # Cada 5 minutos (ajusta si quieres otra frecuencia)
  workflow_dispatch:      # Permite ejecutarlo manualmente desde GitHub

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Llama a la función edge de Supabase
        run: |
          curl -L -X POST 'https://crvxzmsirxorkklsugyh.supabase.co/functions/v1/notify-admin' \
          -H 'Authorization: Bearer sb_publishable_XuRaOIREuFbbnr6JMMwscg_Wf1NVYNU' \
          -H 'apikey: sb_publishable_XuRaOIREuFbbnr6JMMwscg_Wf1NVYNU' \
          -H 'Content-Type: application/json' \
          --data '{"name":"Functions"}'
